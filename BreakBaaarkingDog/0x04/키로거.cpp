#include <bits/stdc++.h>
using namespace std;

int main(void){
    ios::sync_with_stdio(0);
    cin.tie(0);

    int N;
    cin >> N ;
    while(N--){
        string init;
        cin >> init;
        list<char> L;
        auto cursor = L.end();
        cout << *cursor;
        for(auto k : init){
            if(k == '<'){
                continue;
            }
        }
    }
}